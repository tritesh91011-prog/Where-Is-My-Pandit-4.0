import Link from 'next/link';import { owner, services } from '@/lib/data';
export const metadata={title:'Booking Confirmation'};
export default async function Confirmation({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const sp=await searchParams;const service=services.find(s=>s.slug===sp.service)?.title||'Selected Service';const id=sp.id||'WIMP-DEMO';return <section className="py-16"><div className="mx-auto max-w-3xl rounded-[2rem] bg-white p-8 text-center shadow-temple"><div className="mx-auto grid h-20 w-20 place-items-center rounded-full bg-green-100 text-4xl text-green-700">✓</div><h1 className="sanskrit mt-5 text-4xl font-black text-maroon">Booking Created</h1><p className="mt-3 text-maroon/75">Your booking request has been received. Please complete UPI payment to confirm.</p><div className="my-8 rounded-2xl bg-cream p-5 text-left"><p><b>Booking ID:</b> {id}</p><p><b>Service:</b> {service}</p><p><b>Status:</b> Pending Payment Verification</p><p><b>Contact:</b> {owner.phone}</p></div><div className="flex flex-wrap justify-center gap-4"><Link href={`/payment?booking=${id}`} className="rounded-full bg-gradient-to-r from-maroon to-saffron px-7 py-3 font-bold text-white">Pay via UPI</Link><a href={`https://wa.me/91${owner.phone}?text=${encodeURIComponent(`Namaste, my booking ID is ${id}. I have created a booking for ${service}.`)}`} className="rounded-full border border-saffron px-7 py-3 font-bold text-maroon">Send WhatsApp</a></div></div></section>}
